spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/futu_booking
    username: postgres
    password: Hamster1
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true
